#include "structs.h"


int main(int argc, char const *argv[])
{
	/*
	int a,b;

	while (a!=-1 && b!=-1)
		Graph* g = createG();
		while (scanf("%d %d",a,b) && (a!=0 && b!=0)){
			;
		};
		printGraph(g);
		deleteGraph(g);
	*/
	return 0;
}